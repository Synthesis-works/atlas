# Core Constants

This folder holds application-wide global constants and configuration flags:

- `routes.ts`: Platform URL routes and navigation paths
- `events.ts`: Event bus topic definitions
- `permissions.ts`: User role and scope permissions
- `featureFlags.ts`: Modular system feature toggles
