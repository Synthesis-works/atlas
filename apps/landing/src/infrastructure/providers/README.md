# Infrastructure Providers

// TODO: Future backend integration

This folder will house network protocol handlers:
- `restProvider.ts`: FastAPI / REST backend API client
- `websocketProvider.ts`: Realtime log & queue event streaming client
- `sseProvider.ts`: Server-Sent Events stream consumer
