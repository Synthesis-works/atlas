# Infrastructure Adapters

// TODO: Future backend integration

This folder will house engine execution adapters:
- `ollamaAdapter.ts`: Local Ollama model runner adapter
- `openRouterAdapter.ts`: OpenRouter API evaluation adapter
- `huggingfaceAdapter.ts`: HuggingFace Hub dataset & model adapter
- `dockerAdapter.ts`: Local Python sandbox Docker container adapter
